import Background from "./Background";

const Header = () => {
  return (
    <header className="h-28 w-full bg-primary_dark_cyan mb-12">
      <Background />
    </header>
  );
};

export default Header;
